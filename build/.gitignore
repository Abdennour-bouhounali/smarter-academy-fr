# Artefacts de déploiement : construits à la demande, jamais versionnés.
# L'archive contient vendor/ (55 Mo décompressés) et se régénère à l'identique
# depuis composer.lock — voir docs/deployment/HOSTINGER_FILE_MANAGER_DEPLOYMENT.md.
*
!.gitignore
